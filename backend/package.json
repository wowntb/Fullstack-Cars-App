{
  "name": "cars-api",
  "version": "1.0.0",
  "description": "This simple RESTful API allows you to manage a list of cars.",
  "main": "cars_api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix cars-frontend && npm run build --prefix cars-frontend"
  },
  "author": "Nathenale Bedane",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {},
  "engines": {
    "node": "20.11.0",
    "npm": "10.2.4"
  }
}
